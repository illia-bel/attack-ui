{
  "name": "ddos-app",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "backend/*",
    "frontend/*"
  ],
  "scripts": {
    "build-frontend": "yarn workspace attack-ui-app build && yarn workspace attack-ui-site build && sh ./frontend/build-tools/build.sh",
    "fix": "npx prettier \"**/*\" --write"
  }
}
